import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Muhammad Apriansyah Setiawan – Product Owner & IT Consultant",
  description: "Product Owner, IT Project Manager, and IT Consultant with experience delivering SaaS platforms, government solutions, and large-scale digital products.",
  keywords: "Product Owner, Product Manager, IT Project Manager, IT Consultant, Software Engineer, NAS Online, OneKlik BPSDM",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
